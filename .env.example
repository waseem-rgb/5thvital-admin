# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service Role Key (REQUIRED for admin writes)
# Get this from: Supabase Dashboard → Settings → API → service_role key
# ⚠️ NEVER expose this in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Email allowlist for admin access (comma-separated)
# If set, only these emails can access the admin dashboard
# Leave empty to rely on DB roles only
# ADMIN_ALLOWED_EMAILS=admin@example.com,super@example.com

# Optional: Debug logging for admin authorization
# Set to "1" to enable detailed auth logs (server-side only)
# ADMIN_DEBUG=1
